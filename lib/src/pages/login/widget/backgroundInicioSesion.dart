
import 'package:flutter/material.dart';

Widget backgroudInicioSesion(BuildContext context){
  return Container(
        width: double.infinity,
    height: MediaQuery.of(context).size.height * 0.42,
    color: Colors.amber,
    alignment: Alignment.center,
  );
}

